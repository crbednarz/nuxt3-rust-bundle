[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8.1"
mime_guess = "2.0.5"
rust-embed = { version = "8.5.0", features = ["mime-guess"] }
tokio = { version = "1.0", features = ["macros", "rt-multi-thread"] }
